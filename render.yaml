services:
  - type: web
    name: detector-emociones
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port=8080 --server.address=0.0.0.0
    plan: free
    pythonVersion: 3.10.13
